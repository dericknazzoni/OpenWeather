import SwiftUI
import UIKit

// MARK: Custom Colors
extension ShapeStyle where Self == Color {
    public static var primaryBackGroundColor: Color {
        Color(red: 0.20, green: 0.54, blue: 0.901)
    }
}

extension UIColor {
    static let primaryColor = UIColor(red: 0.20, green: 0.54, blue: 0.901, alpha: 1)
}

